# @webnotary/data-model

Shared DynamoDB key builders, normalization, and TypeScript types for WebNotary MVP.

Schema authority: `specs/0/0.001-operational-data-model/data-model.md`

```bash
npm install
npm test
npm run build
```
